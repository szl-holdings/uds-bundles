# UDS Bundle Observatory v2

Source-owned, read-only UDS/Zarf validation and showcase workstream. This branch does not deploy packages, mutate clusters, publish OCI artifacts, or claim Hugging Face runtime readiness.

State boundary: `SOURCE_PR != MERGED != HUB_PUBLISHED != RUNTIME_READY != EXACT_READBACK_VERIFIED`.
